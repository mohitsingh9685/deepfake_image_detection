services:
  backend:
    build: ./backend
    container_name: deepfake_backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: deepfake_frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: always